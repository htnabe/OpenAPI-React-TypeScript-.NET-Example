{
  "name": "Custom Ubuntu Dev Container",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "postCreateCommand": "yarn global add npm-check-updates && cd front && yarn install && cd ../webapi && dotnet restore"
}
